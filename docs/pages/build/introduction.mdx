export default function FitnessGutHealthApp() { const sections = [ { title: 'Workout Plans', desc: 'Muscle gain, fat loss, and beginner workouts.', emoji: '💪', }, { title: 'Digestive Recovery', desc: 'Simple gut-friendly meals and recovery guidance.', emoji: '🥗', }, { title: 'Transformation Story', desc: 'Your bodybuilding comeback journey.', emoji: '🔥', }, { title: 'Daily Motivation', desc: 'Stay focused and consistent every day.', emoji: '⚡', }, ];

return ( <div className="min-h-screen bg-black text-white p-6"> <div className="max-w-5xl mx-auto"> <div className="text-center mb-10"> <h1 className="text-5xl font-bold text-red-500 mb-4"> Rise Again Fitness </h1> <p className="text-gray-300 text-lg"> Fitness • Recovery • Comeback </p> </div>

<div className="grid md:grid-cols-2 gap-6 mb-10">
      {sections.map((item, index) => (
        <div
          key={index}
          className="bg-zinc-900 rounded-3xl p-6 shadow-lg border border-zinc-800"
        >
          <div className="text-4xl mb-4">{item.emoji}</div>
          <h2 className="text-2xl font-semibold mb-2">
            {item.title}
          </h2>
          <p className="text-gray-400">{item.desc}</p>
        </div>
      ))}
    </div>

    <div className="bg-red-600 rounded-3xl p-8 text-center">
      <h2 className="text-3xl font-bold mb-4">
        Your Story Will Inspire Others
      </h2>
      <p className="text-lg mb-6">
        From skinny to stage, through illness and recovery — this app is
        built from real experience.
      </p>

      <button className="bg-white text-black px-6 py-3 rounded-2xl font-bold hover:scale-105 transition">
        Start Transformation
      </button>
    </div>

    <div className="mt-10 text-center text-gray-500 text-sm">
      App Prototype for Android Fitness & Gut Health Coaching
    </div>
  </div>
</div>

); }
